model:
  name: snowflake_JOBS_CUSTOMERAI
  attributes:
    firstName:
      type: string
    lastName:
      type: string
    email:
      type: string
    createdAt:
      type: date
    updatedAt:
      type: date