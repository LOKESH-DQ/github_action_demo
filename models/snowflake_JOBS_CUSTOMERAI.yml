model:
  name: snowflake_JOBS_CUSTOMERAI
  attributes:
    firstName:
      type: string
    email:
      type: string
      format: email
    createdAt:
      type: date
    updatedAt:
      type: date